type User {
  id: ID! @id
  name: String!
}

type Project {
  id: ID! @id
  name: String!
  tasks: [Task!]!
}

type Task {
  id: ID! @id
  title: String!
  project: Project!
  inCompleted: Boolean! @default(value: "false")
}